apiVersion: apps/v1
kind: Deployment
metadata:
  name: adn-parking-back
  namespace: prueba-concepto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adn-parking-back
  template:
    metadata:
      labels:
        app: adn-parking-back
    spec:
      containers:
        - name: parking-docker-back-kb
          image: parkingimages.azurecr.io/parking-docker-back-kb
          ports:
            - containerPort: 8080
          resources:
            limits: 
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 200Mi 
          env:
          - name: DB_DATABASE
            valueFrom:
             secretKeyRef:
                name: db-secret
                key: db_database
          - name: DB_USER
            valueFrom:
             secretKeyRef:
                name: db-secret
                key: db_user
          - name: DB_PASSWORD
            valueFrom:
             secretKeyRef:
                name: db-secret
                key: db_password
        
---
apiVersion: v1
kind: Service
metadata:
  name: adn-parking-back
  namespace: prueba-concepto
spec:
  type: LoadBalancer
  selector:
    app: adn-parking-back
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080