jasypt:
   encryptor:
      password: ${jasyptpwd:secretkey}
      algorithm: PBEWithMD5AndDES

server:
   port: 8080
   servlet:
      context-path: /parking
      session:
         timeout: 21600s

spring:
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      type: com.zaxxer.hikari.HikariDataSource
      jdbcUrl: jdbc:mysql://mysql-server-adn.mysql.database.azure.com:3306/parking?useSSL=true&requireSSL=false&serverTimezone=UTC
      username: daniel@mysql-server-adn
      password: dp_12345
      hikari:
         poolName: pool
